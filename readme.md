rancher-vamp-gateway-agent
==========================

vamp-gateway-agent image based in rancher-base (alpine)

To build

```
docker build -t <repo>/rancher-vamp-gateway-agent:<version> .
```

To run:

```
docker run -it <repo>/rancher-vamp-gateway-agent:<version> 
```

